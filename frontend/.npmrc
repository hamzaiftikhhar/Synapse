# Next 15 lists @playwright/test@^1.51.1 as an optional peer. This repo
# pins 1.48.0 so Chromium still runs on macOS 12 (see ROADMAP Phase 32).
# npm 7+ treats that mismatch as ERESOLVE even though the peer is optional,
# which is what broke the Vercel install.
legacy-peer-deps=true
