"use client";

import PlatformPlaceholder from "@/components/dashboard/platform-placeholder";

export default function Page() {
  return (
    <PlatformPlaceholder
      title="AI monitoring"
      description="Latency, failures, vector searches, SQL queries, and slow requests."
    />
  );
}
