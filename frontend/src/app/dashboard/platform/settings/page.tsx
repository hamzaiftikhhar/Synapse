"use client";

import PlatformPlaceholder from "@/components/dashboard/platform-placeholder";

export default function Page() {
  return (
    <PlatformPlaceholder
      title="Platform settings"
      description="Plans, email/SMS providers, default widget config, feature flags, and global prompts."
    />
  );
}
