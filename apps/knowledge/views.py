from django.shortcuts import render

# Create your views here.
# why we are creating a views.py file and what the views are? because we want to create a view for the knowledge model. 
# what are the views? views are the functions that are used to render the templates. 
# what are the templates? templates are the files that are used to render the views. 
# what are the models? models are the files that are used to store the data. 
# what are the urls? urls are the files that are used to route the requests to the views. 
# what are the admin.py files? admin.py files are used to register the models with the admin site. 
# what are the tests.py files? tests.py files are used to test the models. 
# what are the serializers.py files? serializers.py files are used to serialize the data. 
# what are the permissions.py files? permissions.py files are used to define the permissions for the models. 
# what are the signals.py files? signals.py files are used to define the signals for the models. 

#in comparison to rails, views are the controllers in rails. and in django, views are the functions that are used to render the templates. 